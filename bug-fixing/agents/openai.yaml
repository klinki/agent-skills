interface:
  display_name: "Bug Fixing"
  short_description: "Append-only bug investigation and fix workflow"
  default_prompt: "Use $bug-fixing to investigate this defect, preserve the full bug history, and track each fix attempt until the user confirms the resolution."
